# Assessment 1.3

This repo contains:

1. `index.html` --  CSS styling task based on the provided starter layout.
2. `birds.html` -- JavaScript bird switcher + theme toggle using CSS variables.
3. `css/style.css` --CSS style sheet 

## How to run

1. Open `index.html` in your browser.
2. Open `birds.html` in your browser.

## Rubric proof checklist for what I have completed in my Assignment 1.3

- [x] Semantic layout + correct HTML elements
- [x] CSS variables used
- [x] 5 bird buttons
- [x] Exactly one `<img>` and one `<p>` on birds page
- [x] Button clicks update image + text
- [x] Theme toggle via `data-theme` with 2 palettes (5 variables each)
- [x] No external libraries

## Extra features

- Theme saved in `localStorage`
- Active button highlight